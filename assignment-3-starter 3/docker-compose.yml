version: '3.9'

services:
  api:
    build: ./api
    ports:
      - "3000:3000"
    env_file:
      - ./api/.env
    volumes:
      - ./api:/app
      - /app/node_modules
    working_dir: /app
    command: ["node", "server.js"]
